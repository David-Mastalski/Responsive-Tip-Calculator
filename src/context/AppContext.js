import { createContext } from "react";

export const NumberOfPeopleContext = createContext(null);
export const PeopleErrorContext = createContext(null);
export const BillContext = createContext(null);
export const SelectedTipContext = createContext(null);
export const TipContext = createContext(null);
export const CustomInputValueContext = createContext(null);
